import mx.screens.Form;import mx.controls.Button;import mx.controls.TextInput;class WeatherForm extends Form {		static var symbolOwner:Object = WeatherForm;	static var symbolName:String = "WeatherForm";     	var className:String = "WeatherForm";		/* declare a reference to the 	weather_wsc Web Service Connector */	private var weather_wsc;	/* declare a reference to the 	weather_button Button*/	private var weather_button:Button;	/* declare a reference to the 	zipCode_textInput TextInput*/	private var zipCode_textInput:TextInput;		function onLoad() {		var obj = this;		var weatherButtonListener = new Object();		weatherButtonListener.click = function() {			/* when the form loads onto the screen			trigger the Web Service Connector */			obj.weather_wsc.trigger();		}		weather_button.addEventListener("click", weatherButtonListener);										// add data restrictions		zipCode_textInput.maxChars = 5;		zipCode_textInput.restrict = "0-9";	}}