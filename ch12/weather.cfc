<cfcomponent><cffunction name="getWeatherReport" access="remote" returntype="struct" output="false">  <cfargument name="zipCode" type="numeric" required="true">  <cfset Var weather = "" >  <cfset Var returnStruct = StructNew() >  <cfinvoke method = "GetWeatherInfo" returnvariable = "weather"webservice = "http://www.ejse.com/WeatherService/Service.asmx?wsdl">    <cfinvokeargument name="zipCode" value="#Arguments.ZipCode#">    </cfinvoke>  <cfscript>    returnStruct.location = weather.getLocation();    returnStruct.temperature = weather.getTemprature();    returnStruct.feelsLike = weather.getFeelsLike();    returnStruct.forecast = weather.getForecast();    returnStruct.visibility = weather.getVisibility();    returnStruct.pressure = weather.getPressure();    returnStruct.dewPoint = weather.getDewPoint();    returnStruct.UVIndex = weather.getUVIndex();    returnStruct.humidity = weather.getHumidity();    returnStruct.wind = weather.getWind();    returnStruct.reportedAt = weather.getReportedAt();    returnStruct.lastUpdated = weather.getLastUpdated();   </cfscript>  <cfreturn returnStruct >  </cffunction></cfcomponent>